# Python toolchain for the Tag Force 5 decompilation (PSP / MIPS Allegrex)
# Install in a virtualenv:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt

# Splits the binary into asm/ + assets/ (supports the "psp" platform)
splat64[mips]==0.41.0

# MIPS disassembler with Allegrex support (R4000ALLEGREX) — a splat dependency,
# pinned here for reproducibility
spimdisasm==1.42.2
rabbitizer==1.16.2

# Miscellaneous utilities used by the scripts/tools
pyelftools>=0.31   # reads ELF/PRX headers from Python
colorama>=0.4      # colored output (asm-differ / m2c)
