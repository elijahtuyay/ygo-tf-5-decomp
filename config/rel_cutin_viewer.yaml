# splat config for rel_cutin_viewer.prx — full file coverage.
#
# Generated by scripts/module_survey.py; see config/rel_movie_viewer.yaml
# for the hand-written original this mirrors, including the two gotchas
# encoded here: `header` must be its own top-level segment (splat groups
# bin subsegments of a code segment into the DATA block, i.e. after .text),
# and .text + .sceStub.text must stay in ONE contiguous code segment based
# at file 0x54 / vram 0x0 so func_XXXXXXXX names are offsets from the start
# of the code segment.
#
# Section layout (`readelf -SW`, file offsets):
#   0x000054-0x01CB50  .text                    (0x1CAFC)
#   0x01CB50-0x020158  .sceStub.text            (0x3608)
#   0x020158-0x02015C  .lib.ent.top             (0x4)
#   0x02015C-0x02017C  .lib.ent                 (0x20)
#   0x02017C-0x020180  .lib.ent.btm             (0x4)
#   0x020180-0x020184  .lib.stub.top            (0x4)
#   0x020184-0x02019C  .lib.stub                (0x18)
#   0x02019C-0x0201A0  .lib.stub.btm            (0x4)
#   0x0201A0-0x0201D4  .rodata.sceModuleInfo    (0x34)
#   0x0201D4-0x020204  .rodata.sceResident      (0x30)
#   0x020204-0x021D08  .rodata.sceNid           (0x1B04)
#   0x021D08-0x021D68  .rodata.sceVstub         (0x60)
#   0x021D68-0x025AD4  .data                    (0x3D6C)
#   0x025AD4-0x02F2E0  section header table + .rel.* + .shstrtab + .comment
#
# Ranges sum to 0x2F2E0 == 193248 == the file's exact size.

name: rel_cutin_viewer
sha1: 06ffe7d3a246e1428a4a268c624b730d7797f849
options:
  platform: psp
  basename: rel_cutin_viewer
  base_path: ..
  target_path: iso_extracted/PSP_GAME/USRDIR/gmodule/rel_cutin_viewer.prx
  asm_path: asm/rel_cutin_viewer
  ld_script_path: build/rel_cutin_viewer.ld
  compiler: GCC          # placeholder to drive the disassembler; the game uses MWCC (see docs/06)
  find_file_boundaries: True
  section_order: [".text", ".data", ".rodata", ".bss"]

segments:
  - [0x0, bin, header]   # ELF header + program headers, file 0x0-0x54
  - name: rel_cutin_viewer
    type: code
    start: 0x54
    vram: 0x0
    subsegments:
      - [0x54, asm, text]          # vram 0x0-0x1CAFC: the code to decompile
      - [0x1CB50, asm, sceStub_text]  # vram 0x1CAFC-0x20104: import-stub trampolines
      - [0x20158, bin, lib_tables]   # .lib.ent.top + .lib.ent + .lib.ent.btm + .lib.stub.top + .lib.stub + .lib.stub.btm
      - [0x201A0, bin, module_info]   # .rodata.sceModuleInfo + .rodata.sceResident
      - [0x20204, bin, sceNid]   # .rodata.sceNid
      - [0x21D08, bin, sceVstub]   # .rodata.sceVstub
      - [0x21D68, bin, data]   # .data
      - [0x25AD4, bin, elf_meta]   # section header table + .rel.* + .shstrtab + .comment
  - [0x2F2E0]   # sentinel: total file size
